{
  "name": "@runethread/hosted",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.19.0",
  "engines": {
    "node": "24.20.0"
  },
  "scripts": {
    "types:check": "wrangler types --check",
    "typecheck": "tsc --noEmit && tsc -p test/tsconfig.json --noEmit",
    "test": "vitest run",
    "release:probe": "node scripts/release_identity.mjs --mode ci --version v0.0.0-ci --probe",
    "check": "npm run types:check && npm run typecheck && npm test && npm run release:probe"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.5",
    "typescript": "5.8.3",
    "vitest": "4.1.11",
    "wrangler": "4.129.1"
  }
}
